model_list:
  - model_name: phi # Redirect to tinydolphin for smaller size
    litellm_params:
      model: ollama/tinydolphin
      api_base: http://ollama:11434

litellm_params:
  drop_params: True

router_settings:
  num_retries: 2
  timeout: 60
